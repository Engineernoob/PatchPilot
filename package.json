{
  "name": "patchpilot",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @patchpilot/web",
    "build:web": "npm run build --workspace @patchpilot/web",
    "typecheck:web": "npm run typecheck --workspace @patchpilot/web"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
